#include <A_star.h>

// Default constructor
AStar::AStar() 
{
}

std::vector<Cell> AStar::findPath(const Grid& grid, const Cell& start, const Cell& goal)
{
    cout << "test" << endl;
    std::vector<Cell> test;
    return test;
}
